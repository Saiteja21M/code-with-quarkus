apiVersion: v1
kind: Service
metadata:
  name: code-with-quarkus
spec:
  selector:
    app: code-with-quarkus
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
  type: NodePort